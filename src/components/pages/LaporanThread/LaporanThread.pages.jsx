import React from "react";
import { LaporanContent } from "../../organism";
import "../pages.css";

const LaporanThread = () => {
  return (
    <div>
      <LaporanContent />
    </div>
  );
};

export default LaporanThread;
